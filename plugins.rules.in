##
# source should be one of:
#
# - "*"      		Data from any source (logfile, UDP listener, etc) is sent to this plugin.
# - "address:port/udp" 	Data received from this listener is sent to the configured plugin.
# - "/path/to/logfile" 	Data from this logfile is sent to the configured plugin.
#
#
# source	plugin-name-list      pcre-options	regex
#
# *		Debug		      -			.*
  * 		Pcre		      -			.*

